type RiderStats {
  id: ID!
  category: Category!
  club: Club!
  firstPlaces: Int!
  gcLeaderStages: Int!
  gcPoints: Int!
  isGcWinner: Boolean!
  rider: Rider!
  secondPlaces: Int!
  stagesPlaced: Int!
  stagesRaced: Int!
  thirdPlaces: Int!
  totalPoints: Int!
}

type Query {
  riderStats(riderId: ID!): RiderStats!
}
